<h1>Keyboard Family</h1>

<%= link_to 'Home', root_path %>

<table>
  <thead>
  </thead>

  <tbody>
    <% Subcategory.where(category_id: '5').each do |name|%>
      <tr>
        <td><%= link_to name.instrument, "/keyboard/#{name.id}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>